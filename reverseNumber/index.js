const reverse = require('./reverse');

// Numbers
const n1 = 62,
      n2 = -62,
      n3 = -100;

// Check reverse
console.log(`${n1} reversed to: ${reverse(n1)}`);
console.log(`${n2} reversed to: ${reverse(n2)}`);
console.log(`${n3} reversed to: ${reverse(n3)}`);
